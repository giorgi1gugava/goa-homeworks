let score = 95;

let result = score >= 90 ? "Excellent" 
             : score >= 70 ? "Good" 
             : "Fail";

console.log(result);  

score = 85;
result = score >= 90 ? "Excellent" 
         : score >= 70 ? "Good" 
         : "Fail";
console.log(result);

score = 60;
result = score >= 90 ? "Excellent" 
         : score >= 70 ? "Good" 
         : "Fail";
console.log(result);  